name: Build Android APK

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Install dependencies
        run: npm ci

      - name: Build web app
        run: npm run build

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Build Debug APK
        working-directory: android
        run: ./gradlew assembleDebug

      - name: Build Release APK
        working-directory: android
        run: ./gradlew assembleRelease

      - name: Get version from tag
        id: tag_name
        run: echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Rename APK files
        run: |
          mv android/app/build/outputs/apk/debug/app-debug.apk android/app/build/outputs/apk/debug/gemini-tagger-pro-${{ steps.tag_name.outputs.tag }}-debug.apk
          mv android/app/build/outputs/apk/release/app-release-unsigned.apk android/app/build/outputs/apk/release/gemini-tagger-pro-${{ steps.tag_name.outputs.tag }}-release.apk

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: Release ${{ steps.tag_name.outputs.tag }}
          body: |
            ## Gemini Tagger Pro ${{ steps.tag_name.outputs.tag }} 🚀

            ### 📱 Android APK 下载

            | 版本 | 说明 |
            |------|------|
            | **Debug APK** | 调试版本，可直接安装测试 |
            | **Release APK** | 发布版本（未签名） |

            ### ✨ 功能特性
            - 🖼️ 批量图片标签生成（Google Gemini AI）
            - 🔄 多节点支持（Google SDK + OpenAI 兼容）
            - ⚡ 并发处理与自动重试
            - 📦 图片压缩与结果验证
            - 📥 ZIP 导入/导出功能
            - 🎯 优化的批处理算法

            ### 📲 安装说明
            1. 下载 `gemini-tagger-pro-${{ steps.tag_name.outputs.tag }}-debug.apk`
            2. 在 Android 设备上允许安装未知来源应用
            3. 打开 APK 文件进行安装
            4. 在设置中配置您的 Gemini API Key

            ### ⚠️ 注意事项
            - Release APK 未签名，如需发布到应用商店请自行签名
            - 最低支持 Android 5.0 (API 21)

          files: |
            android/app/build/outputs/apk/debug/gemini-tagger-pro-${{ steps.tag_name.outputs.tag }}-debug.apk
            android/app/build/outputs/apk/release/gemini-tagger-pro-${{ steps.tag_name.outputs.tag }}-release.apk
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Debug APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk-${{ steps.tag_name.outputs.tag }}
          path: android/app/build/outputs/apk/debug/*.apk
          retention-days: 90

      - name: Upload Release APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-apk-${{ steps.tag_name.outputs.tag }}
          path: android/app/build/outputs/apk/release/*.apk
          retention-days: 90
